spring:
  application:
    name: bank-api

  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/banking_db

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true